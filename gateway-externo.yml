server:
  port: 8084
  
spring:
  cloud:
    gateway:
      routes:
      - id: mgd
        uri: http://localhost:8081
        predicates:
        - Path=/msw-e/**
        filters:
        - RewritePath=/msw-e/(?<segmento>.*), /mgd-b/$\{segmento}
